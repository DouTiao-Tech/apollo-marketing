spring:
  mvc:
    format:
      date: yyyy-MM-dd
      date-time: yyyy-MM-dd HH:mm:ss
      time: HH:mm:ss
  jackson:
    mapper:
      PROPAGATE_TRANSIENT_MARKER: true
    parser:
      IGNORE_UNDEFINED: true